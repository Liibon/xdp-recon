[package]
name = "xdp-recon"
version = "0.1.0"
edition = "2021"
description = "XDP packet drop reconciliation test harness"
license = "MIT"

[dependencies]
anyhow = "1"
ctrlc = "3"
libbpf-rs = "0.23"
nix = { version = "0.27", features = ["net"] }

[build-dependencies]
libbpf-cargo = "0.23"
